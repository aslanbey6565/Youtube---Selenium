package video1;

public class SoftAssertion {


    // Assertion = bir uygulamanin istedigimiz gibi calistigini dogrulamak veya test emektir.
    // JUnit Assertion == bu method ile if-else methoduna gerek kalmaz
    // Assert.assertTrue(actualword.contains(expectedword));
    // dezavantaj == bir testi tekrar tekrar calistirmamizi gerektirebilir


    // Soft Assert = assertion yaptigimizda bir hata buldugunda yine de calismaya devam eder
    // SoftAssert softAssert = new SoftAssert();
    // softAssert.assertTrue();
    // softAssert.assertTrue();
    // softAssert.assertAll();

    // biz assertAll () yazana kadar devam eder. assertAll () ile sonlandirir ve raporu aliriz


}
